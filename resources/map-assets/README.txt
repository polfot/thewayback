The Way Back — pixel-art parchment map assets

All individual PNG files have transparent backgrounds and native 1:1 pixels.
Use imageSmoothingEnabled = false / image-rendering: pixelated when scaling.
sprite_sheet.png uses 40x40 transparent cells; sprites.json gives exact rects.
